<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- H-5f: WRITE_EXTERNAL_STORAGE kaldırıldı — kodda kullanılmıyor; maxSdk 28
         gereksiz bir izin yüzeyiydi (güvenlik ekibi önerisi). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- H-5: SEND_RESULT özel izni bildiren pakete göre ad alanlandı ve signature
         seviyesine çekildi. Artık yalnızca aynı imza ile imzalanmış uygulamalar
         (ör. AlgorDroid motoru com.magicv3.scanner3d.permission.SEND_RESULT
         iznini taşımalı) bu receiver'a broadcast gönderebilir. -->
    <permission
        android:name="com.magicv3.scanner3d.permission.SEND_RESULT"
        android:protectionLevel="signature" />

    <queries>
        <package android:name="com.algordroid.engine" />
    </queries>

    <application
        android:name=".MagicScannerApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MagicScanner">
        <!-- B-3: ARCore isteğe bağlı (optional). Cihaz ARCore desteklemiyorsa uygulama
             yine de kurulup çalışabilir; ARSession oluşturulamazsa fallback gösterilir. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MagicScanner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".infra.ingestion.AlgorDroidResultReceiver"
            android:exported="true"
            android:permission="com.magicv3.scanner3d.permission.SEND_RESULT">
            <intent-filter>
                <action android:name="com.algordroid.engine.ACTION_PROCESSING_PROGRESS" />
                <action android:name="com.algordroid.engine.ACTION_PROCESSING_COMPLETE" />
                <action android:name="com.algordroid.engine.ACTION_PROCESSING_ERROR" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
